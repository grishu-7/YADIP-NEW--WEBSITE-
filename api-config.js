// Set this to your backend URL when frontend and backend are deployed separately.
// Example: window.API_BASE_URL = "https://your-backend.example.com";
// For a single Render/VPS deployment leave it blank.
window.API_BASE_URL = "";
